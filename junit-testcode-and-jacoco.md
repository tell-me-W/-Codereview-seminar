# JUnit Test Code와 JaCoCo

## 1. 테스트 코드가 왜 필요한가?

우리 시스템은 오랫동안 운영되어 온 레거시 시스템이고, 테스트 코드가 거의 없는 상태에서 기능 추가와 버그 수정을 해왔다.

테스트 코드가 없으면 변경의 영향 범위를 사람이 직접 기억하고 확인해야 한다. 작은 수정처럼 보여도 실제로는 다른 기능을 깨뜨릴 수 있고, 코드 리뷰에서도 "이 변경이 정말 안전한가?"를 확인하기 어렵다.

테스트 코드는 단순히 버그를 찾기 위한 코드가 아니다.

- 변경해도 되는지 확인하는 안전장치
- 기존 동작을 설명하는 문서
- 코드 리뷰의 신뢰도를 높이는 근거
- 배포 전 불안감을 줄여주는 자동 검증 수단

특히 레거시 시스템에서는 테스트 코드가 더 중요하다. 이미 복잡해진 코드일수록 사람의 기억과 수동 테스트만으로 품질을 지키기 어렵기 때문이다.

---

## 2. JUnit이란?

JUnit은 Java에서 테스트 코드를 작성하고 실행하기 위한 대표적인 테스트 프레임워크다.

현재 우리 환경에서는 JDK 버전 제약 때문에 JUnit4를 사용해야 하지만, 앞으로 JDK 버전이 올라가면 JUnit5를 사용할 수도 있다. 다만 중요한 것은 JUnit4냐 JUnit5냐가 아니라, 테스트 코드를 통해 코드의 동작을 자동으로 검증하는 습관이다.

JUnit 테스트는 보통 다음 흐름을 가진다.

```java
@Test
public void calculateTotalPrice_discountApplied_returnsDiscountedPrice() {
    // given
    int price = 10000;
    int discountRate = 10;

    // when
    int result = calculator.calculate(price, discountRate);

    // then
    assertEquals(9000, result);
}
```

테스트 코드는 보통 `given / when / then` 구조로 작성하면 읽기 쉽다.

- `given`: 테스트에 필요한 조건 준비
- `when`: 테스트 대상 메서드 실행
- `then`: 기대한 결과인지 검증

좋은 테스트는 실패했을 때 "무엇이 깨졌는지"를 빠르게 알려줘야 한다.

---

## 3. 테스트 코드는 언제 작성해야 할까?

가장 이상적인 방식은 기능을 만들기 전에 테스트를 먼저 생각하는 것이다. 하지만 레거시 시스템에서는 처음부터 모든 코드를 테스트로 덮는 것이 현실적이지 않다.

우리 팀에서는 다음 순서로 시작하는 것이 좋다.

1. 신규 기능을 작성할 때 테스트를 함께 작성한다.
2. 버그를 수정할 때, 먼저 버그를 재현하는 테스트를 추가한다.
3. 자주 변경되는 핵심 비즈니스 로직부터 테스트를 만든다.
4. 외부 연동, DB, 화면보다 순수한 계산/판단 로직을 먼저 테스트한다.

처음부터 모든 코드를 테스트하려고 하면 부담이 크다. 중요한 것은 "테스트가 없던 상태"에서 "변경할 때마다 테스트가 조금씩 늘어나는 상태"로 전환하는 것이다.

---

## 4. FIRST 원칙

좋은 테스트 코드는 FIRST 원칙을 만족해야 한다.

### Fast

테스트는 빨라야 한다. 테스트 실행이 오래 걸리면 개발자가 자주 실행하지 않게 된다.

DB, 외부 API, 파일 시스템 등에 많이 의존하는 테스트는 느려질 수 있다. 가능하면 핵심 로직은 빠르게 검증할 수 있도록 분리하는 것이 좋다.

### Independent

테스트는 서로 독립적이어야 한다. A 테스트가 먼저 실행되어야 B 테스트가 성공하는 구조는 좋지 않다.

테스트 실행 순서에 따라 성공/실패가 달라지면 원인을 찾기 어렵다.

### Repeatable

테스트는 언제 어디서 실행해도 같은 결과가 나와야 한다. 개발자 PC, CI 서버, 다른 시간대, 다른 날짜에 따라 결과가 달라지면 신뢰할 수 없다.

현재 시간, 랜덤값, 외부 API 응답처럼 변할 수 있는 값은 테스트에서 통제해야 한다.

### Self-Validating

테스트는 스스로 성공/실패를 판단해야 한다. 테스트 실행 후 사람이 로그를 보고 판단해야 한다면 좋은 자동 테스트가 아니다.

`assertEquals`, `assertTrue`, `assertThrows` 같은 검증 코드를 통해 기대 결과를 명확히 표현해야 한다.

### Timely

테스트는 너무 늦지 않게 작성해야 한다. 기능 개발이 끝나고 한참 뒤에 테스트를 작성하면 코드 구조가 이미 테스트하기 어려운 상태가 되어 있을 가능성이 높다.

적어도 기능 추가나 버그 수정 시점에는 관련 테스트를 함께 작성하는 것이 좋다.

---

## 5. Mock이란?

테스트 대상 코드가 다른 객체나 외부 시스템에 의존하고 있을 때, 그 의존성을 실제 객체 대신 가짜 객체로 대체하는 것을 Mock이라고 한다.

예를 들어 주문 금액을 계산하는 로직을 테스트하고 싶은데, 그 과정에서 DB 조회나 외부 API 호출이 필요하다면 테스트가 복잡하고 느려진다. 이때 DB나 API를 실제로 호출하지 않고, 미리 정해진 값을 반환하는 Mock 객체를 사용할 수 있다.

Mock을 사용하면 다음 장점이 있다.

- 테스트 대상 로직에 집중할 수 있다.
- 외부 시스템 장애와 무관하게 테스트할 수 있다.
- 느린 의존성을 제거해 테스트를 빠르게 만들 수 있다.
- 특정 예외 상황을 쉽게 재현할 수 있다.

대표적으로 Mockito 같은 라이브러리를 사용한다.

```java
when(userRepository.findById(1L)).thenReturn(user);

User result = userService.findUser(1L);

assertEquals("kim", result.getName());
```

Mock은 편리하지만, 남용하면 테스트가 실제 동작과 멀어질 수 있다. 따라서 "무엇을 Mock으로 대체할지"를 신중하게 판단해야 한다.

---

## 6. PowerMock이 필요한 이유

일반적인 Mockito만으로는 테스트하기 어려운 레거시 코드가 있다.

예를 들면 다음과 같은 코드다.

- static 메서드를 직접 호출하는 코드
- private 메서드에 중요한 로직이 숨어 있는 코드
- 생성자 내부에서 무거운 의존성을 직접 생성하는 코드
- final 클래스나 final 메서드에 강하게 의존하는 코드

레거시 시스템에서는 이런 구조가 흔하다. 이 경우 테스트를 작성하려고 해도 의존성을 바꿔 끼우기 어렵기 때문에 PowerMock 같은 도구가 필요할 수 있다.

PowerMock은 static, private, constructor 호출 등을 제어할 수 있게 해주는 강력한 테스트 도구다. 하지만 강력한 만큼 남용하면 테스트가 구현 세부사항에 너무 강하게 묶일 수 있다.

따라서 PowerMock은 다음처럼 바라보는 것이 좋다.

- 레거시 코드를 테스트하기 위한 현실적인 도구
- 당장 구조를 바꾸기 어려울 때 사용하는 임시 다리
- 장기적으로는 PowerMock 없이 테스트 가능한 구조로 개선하는 것이 목표

즉, PowerMock을 쓴다는 것은 "이 코드가 테스트하기 어려운 구조"라는 신호이기도 하다.

---

## 7. JaCoCo란?

JaCoCo는 Java 코드의 테스트 커버리지를 측정하는 도구다.

테스트를 실행했을 때 어떤 코드가 실행되었고, 어떤 코드는 실행되지 않았는지를 리포트로 보여준다.

JaCoCo를 통해 확인할 수 있는 대표적인 지표는 다음과 같다.

- 라인 커버리지: 코드 라인 중 테스트로 실행된 비율
- 브랜치 커버리지: if, switch 같은 분기 중 테스트된 비율
- 메서드/클래스 커버리지: 테스트로 실행된 메서드와 클래스 비율

하지만 커버리지가 높다고 해서 테스트 품질이 반드시 좋은 것은 아니다.

예를 들어 assert가 없는 테스트도 커버리지는 올릴 수 있다. 반대로 커버리지가 낮아도 핵심 비즈니스 로직을 정확히 검증하고 있다면 더 가치 있는 테스트일 수 있다.

JaCoCo는 목표 점수를 맞추기 위한 도구가 아니라, 테스트가 부족한 영역을 찾기 위한 진단 도구로 봐야 한다.

---

## 8. 커버리지 숫자를 어떻게 볼 것인가?

커버리지 80%, 90% 같은 숫자는 참고 지표일 뿐이다. 숫자 자체가 품질을 보장하지 않는다.

중요한 질문은 다음과 같다.

- 장애가 나면 큰 문제가 되는 로직이 테스트되고 있는가?
- 자주 변경되는 코드가 테스트되고 있는가?
- 복잡한 조건 분기가 테스트되고 있는가?
- 버그가 났던 부분에 재발 방지 테스트가 있는가?
- 테스트가 실패했을 때 원인을 쉽게 알 수 있는가?

우리 팀에서는 처음부터 높은 커버리지를 목표로 하기보다, 위험도가 높은 코드부터 테스트를 추가하는 방식이 현실적이다.

---

## 9. 우리 팀의 테스트 코드 작성 방향

현재 시스템은 테스트 코드가 거의 없는 상태이므로, 한 번에 모든 코드를 테스트 대상으로 삼기 어렵다. 따라서 점진적으로 테스트를 늘려가는 방향이 적절하다.

우선순위는 다음과 같다.

1. 신규 개발 코드는 가능한 테스트를 함께 작성한다.
2. 버그 수정 시에는 재현 테스트를 추가한다.
3. 핵심 비즈니스 로직을 우선 테스트한다.
4. static/private 의존성이 많은 레거시 코드는 필요 시 PowerMock을 사용한다.
5. 리팩토링 가능한 영역은 점진적으로 Mock이 쉬운 구조로 개선한다.
6. JaCoCo로 테스트 사각지대를 확인한다.

테스트 코드는 개발 속도를 늦추는 작업이 아니라, 장기적으로 변경 비용을 낮추는 작업이다. 특히 레거시 시스템에서는 테스트 코드가 쌓일수록 기능 수정, 리뷰, 배포에 대한 신뢰도가 높아진다.

---

## 10. 정리

테스트 코드는 선택 사항이 아니라, 안정적인 변경을 위한 기반이다.

JUnit은 테스트를 실행하기 위한 도구이고, Mock은 테스트 대상의 의존성을 제어하기 위한 방법이다. PowerMock은 레거시 코드처럼 테스트하기 어려운 구조를 다루기 위한 현실적인 도구다. JaCoCo는 테스트가 충분한지 확인하기 위한 커버리지 진단 도구다.

우리의 목표는 모든 코드를 한 번에 완벽하게 테스트하는 것이 아니다.

변경하는 코드에 테스트를 추가하고, 중요한 로직부터 보호하며, 테스트 가능한 구조로 조금씩 개선하는 것이다.

---

## 세미나 진행 중 던져볼 질문

- 이 코드 수정하고 배포할 때 불안했던 경험이 있었나요?
- 리뷰할 때 테스트가 있으면 어떤 점이 달라질까요?
- 커버리지 90%인데 assert가 없다면 좋은 테스트일까요?
- PowerMock이 필요하다는 건 코드 구조 관점에서 어떤 신호일까요?
