# 코드 스멜: 리팩토링이 필요한 신호를 읽는 법

## 1. 코드 스멜이란 무엇인가?

코드 스멜(Code Smell)은 코드를 읽었을 때 **구조를 더 살펴봐야 할 것 같은 신호**다.

코드 스멜은 "이 코드는 틀렸다"라는 판정이 아니다. 코드가 커지고 변경이 누적될 때 구조적 부담이 생겼는지, 변경 비용이 커졌는지, 테스트하기 어려운 이유가 숨어 있는지 질문을 시작하게 해준다.

예를 들어 긴 메서드는 눈에 잘 띄는 스멜이다. 하지만 긴 메서드가 항상 잘못된 것은 아니다. 흐름이 단순하고 한 책임을 자연스럽게 설명한다면 유지할 수도 있다. 반대로 짧은 메서드라도 외부 상태를 과하게 건드리고 숨은 분기가 많다면 더 위험할 수 있다.

이전 세미나와 연결하면 코드 스멜은 다음 순서에 놓인다.

1. 테스트는 리팩토링 전에 기존 동작을 보호하는 안전망이다.
2. 코드 스멜은 어디를 먼저 살펴볼지 알려주는 관찰 포인트다.
3. SOLID와 디자인 패턴은 원인을 이해한 뒤 선택할 수 있는 개선 도구다.

즉 코드 스멜을 본다고 바로 패턴을 넣는 것이 아니다. 먼저 "왜 이 구조가 바꾸기 어렵고 읽기 어려운가?"를 확인해야 한다.

---

## 2. 코드 스멜을 볼 때 주의할 점

### 2.1 스멜은 규칙 위반 목록이 아니다

`Long Method`를 "10줄이 넘으면 무조건 분리"처럼 기계적으로 다루면 오히려 흐름이 찢어진다. 숫자 기준은 질문을 시작하는 힌트로 쓰고, 책임과 변경 이유를 함께 본다.

### 2.2 개선보다 원인 확인이 먼저다

중복 코드가 보인다고 바로 추상 클래스를 만들면 서로 다른 변경 이유를 억지로 묶을 수 있다. 스멜을 발견한 뒤에는 다음을 확인한다.

- 이 코드는 자주 바뀌는가?
- 변경할 때 관련 없는 코드도 함께 바뀌는가?
- 테스트 준비가 과하게 어려운가?
- 장애나 버그가 반복되는 경로인가?
- 분리하면 변경 범위와 이해 비용이 실제로 줄어드는가?

### 2.3 경계 모델은 예외가 될 수 있다

`Data Class`는 객체가 데이터만 들고 행동은 다른 곳에 몰려 있을 때 의심할 만한 스멜이다. 하지만 DTO, API 요청/응답 모델, 메시지 변환 결과처럼 **경계에서 데이터를 옮기는 모델**은 데이터 중심 구조가 자연스러울 수 있다.

### 2.4 주석 자체가 나쁜 것은 아니다

`Comments` 스멜은 주석을 쓰지 말라는 뜻이 아니다. 업무 배경, 위험한 제약, 외부 시스템의 계약을 설명하는 주석은 가치가 있다. 다만 코드가 무엇을 하는지 장문의 주석 없이는 알 수 없다면 이름, 메서드 분리, 책임 경계가 흐린지 살펴본다.

---

## 3. 원문 검증 메모와 분류 한눈에 보기

현재 `Code-Smells.md`는 Refactoring.Guru의 Code Smells 카탈로그와 같은 다섯 분류를 사용한다.

| 분류 | 관찰 포인트 |
| --- | --- |
| Bloaters | 코드, 메서드, 클래스가 너무 커져 다루기 어려움 |
| Object-Orientation Abusers | 객체지향 구조를 어색하게 사용함 |
| Change Preventers | 한 변경이 여러 변경을 끌고 다님 |
| Dispensables | 없어지거나 줄어들면 더 읽기 쉬운 요소 |
| Couplers | 결합도나 과도한 위임이 구조를 흐림 |

원문에 들어 있는 스멜 이름과 분류는 전반적으로 카탈로그 요약과 잘 맞는다. 다만 Refactoring.Guru의 현재 카탈로그에는 `Incomplete Library Class`도 함께 나타나며, 현재 `Code-Smells.md`에는 포함되어 있지 않다. 이번 문서는 원문 범위를 우선 따라가되, 원문이 **전체 리팩토링 카탈로그의 완전한 해설서가 아니라 요약본**이라는 점을 전제로 한다.

---

## 4. Bloaters: 너무 커져서 다루기 어려운 코드

Bloaters는 처음부터 거대한 구조로 태어나기보다 기능 추가와 예외 처리 누적으로 점점 커지는 경우가 많다.

### Long Method (긴 메서드)

- 의미: 한 메서드 안에 여러 판단, 계산, 저장, 외부 호출 흐름이 함께 들어 있다.
- 살펴볼 질문: 메서드 이름 하나로 전체 책임을 설명할 수 있는가? 테스트하려는 핵심 로직보다 준비 코드가 더 길어지는가?
- 개선 방향: 계산, 검증, 변환, 외부 호출을 의미 있는 단위로 추출한다. 분리 후 이름이 흐름을 설명하는지 확인한다.

### Large Class (큰 클래스)

- 의미: 필드, 메서드, 의존성, 변경 이유가 한 클래스에 과하게 모여 있다.
- 살펴볼 질문: 이 클래스가 수신, 검증, 계산, 저장, 응답 생성을 모두 맡고 있는가?
- 개선 방향: 자주 함께 바뀌는 책임부터 분리한다. God Class는 한 번에 없애기보다 변경 지점 주변부터 줄인다.

### Primitive Obsession (원시 타입 집착)

- 의미: 금액, 전화번호, 코드값, 범위처럼 의미 있는 값을 `String`, `int`, 상수 조합으로만 다룬다.
- 살펴볼 질문: 같은 검증과 포맷 규칙이 여러 곳에 퍼져 있는가? 타입이 의미를 설명하지 못해 파라미터 순서를 자주 헷갈리는가?
- 개선 방향: 값 객체, enum, 전용 파라미터 객체를 고려한다. 단순 전달 값까지 무조건 감싸지는 않는다.

### Long Parameter List (긴 파라미터 목록)

- 의미: 메서드 호출에 너무 많은 값이 필요하다.
- 살펴볼 질문: 파라미터 일부가 항상 함께 움직이는가? 호출부가 실제 의도를 드러내지 못하는가?
- 개선 방향: 파라미터 객체를 도입하거나, 이미 가진 객체에서 가져올 수 있는 값을 다시 넘기고 있지 않은지 본다.

### Data Clumps (데이터 뭉치)

- 의미: 같은 변수 묶음이 여러 메서드나 클래스에 반복해서 등장한다.
- 살펴볼 질문: 이 값들은 항상 함께 전달되고 같은 검증을 공유하는가?
- 개선 방향: 묶음 자체에 이름을 주고 객체로 표현한다. 예를 들어 DB 접속 옵션, 기간 범위, 주문 식별 정보는 하나의 모델이 될 수 있다.

---

## 5. Object-Orientation Abusers: 객체지향을 어색하게 사용하는 코드

이 분류는 객체가 책임을 나누는 대신 조건문, 상속, 임시 상태에 지나치게 의존할 때 잘 보인다.

### Switch Statements (복잡한 switch/조건 분기)

- 의미: 타입이나 업무 구분에 따라 같은 조건 분기가 계속 늘어난다.
- 살펴볼 질문: 새 타입을 넣을 때 같은 `if` 또는 `switch`를 여러 곳에서 고쳐야 하는가?
- 개선 방향: 분기 축이 자주 확장된다면 Strategy, 다형성, 핸들러 등록 구조를 고려한다. 단순한 한두 조건까지 억지로 추상화하지 않는다.

### Temporary Field (임시 필드)

- 의미: 특정 시점에서만 의미 있는 필드가 객체에 남아 다른 시점에는 비어 있다.
- 살펴볼 질문: 필드가 null인 상태를 늘 설명해야 하는가? 호출 순서에 따라 객체의 유효성이 달라지는가?
- 개선 방향: 필요한 시점의 별도 객체나 메서드 로컬 값으로 옮겨 수명과 책임을 줄인다.

### Refused Bequest (거부된 상속)

- 의미: 하위 클래스가 상위 클래스의 일부 기능만 억지로 물려받고 나머지는 사용하지 않거나 예외로 막는다.
- 살펴볼 질문: "is-a" 관계보다 코드 재사용 때문에 상속한 것은 아닌가?
- 개선 방향: 조합, 위임, 더 작은 인터페이스를 검토한다. 상위 타입의 계약을 깨는 하위 타입은 교체 가능성을 해친다.

### Alternative Classes with Different Interfaces (다른 인터페이스의 유사 클래스)

- 의미: 비슷한 역할을 하는 클래스들이 서로 다른 메서드 이름과 호출 규칙을 가진다.
- 살펴볼 질문: 사용자가 구현체마다 다른 호출법을 기억해야 하는가?
- 개선 방향: 공통 역할을 표현하는 이름과 인터페이스를 맞추거나 Adapter로 경계를 정리한다.

---

## 6. Change Preventers: 변경을 비싸게 만드는 코드

이 분류는 코드를 수정할 때 특히 아프게 느껴진다. 한 변경이 어느 범위까지 번지는지 보여준다.

### Divergent Change (산발적 변경)

- 의미: 하나의 클래스가 서로 다른 이유로 계속 바뀐다.
- 살펴볼 질문: 상품 타입 추가, 출력 형식 변경, 저장 방식 변경이 같은 클래스의 서로 다른 메서드를 건드리는가?
- 개선 방향: 변경 이유별로 책임을 나눈다. SRP를 적용할 때 자주 만나는 스멜이다.

### Shotgun Surgery (산탄총 수술)

- 의미: 하나의 요구사항을 반영하려고 여러 클래스에 작은 수정을 흩뿌려야 한다.
- 살펴볼 질문: 같은 업무 규칙을 바꿀 때 파일 검색부터 해야 하는가?
- 개선 방향: 변경 규칙을 한 책임 아래로 모은다. 정책 객체, 공통 계산기, 경계 Facade 같은 응집 지점을 찾는다.

### Parallel Inheritance Hierarchies (평행 상속 계층)

- 의미: 한 계층에 하위 타입을 추가하면 다른 계층에도 대응 하위 타입을 추가해야 한다.
- 살펴볼 질문: 두 상속 트리가 사실상 함께 움직이는가?
- 개선 방향: 결합된 계층을 합리적인 책임 단위로 재구성하거나 조합으로 바꿔 독립 확장을 가능하게 한다.

---

## 7. Dispensables: 없어지면 더 읽기 쉬운 코드

이 분류는 "무언가를 더 추가"하기보다 "불필요한 것을 줄이는" 쪽에서 효과가 크다.

### Comments (주석)

- 의미: 코드의 의도를 설명하기 위해 장문의 주석이 계속 필요하다.
- 살펴볼 질문: 주석이 업무 배경을 설명하는가, 아니면 이름과 구조가 설명하지 못한 동작을 대신 말하는가?
- 개선 방향: 코드가 의도를 드러내도록 이름과 메서드를 개선한다. 제약과 배경을 설명하는 좋은 주석은 남긴다.

### Duplicate Code (중복 코드)

- 의미: 같은 로직 또는 같은 형태의 코드 조각이 반복된다.
- 살펴볼 질문: 정말 같은 변경 이유를 공유하는가, 아니면 우연히 지금만 닮았는가?
- 개선 방향: 함께 바뀌는 중복은 추출한다. 아직 변화 방향이 다른 코드라면 서둘러 공통화하지 않는다.

### Lazy Class (게으른 클래스)

- 의미: 유지 비용에 비해 역할이 지나치게 작거나 더 이상 존재 이유가 약하다.
- 살펴볼 질문: 이 클래스가 이름만 나누고 실제로는 위임만 하는가?
- 개선 방향: 주변 책임에 합치거나 불필요한 층을 제거한다. 경계 보호용 Adapter나 의도적인 Facade라면 역할을 다시 확인한다.

### Data Class (데이터 클래스)

- 의미: 필드와 getter/setter만 있고 행동은 다른 클래스가 모두 가진다.
- 살펴볼 질문: 이 데이터가 도메인 객체인데 중요한 규칙이 외부 서비스에만 있는가?
- 개선 방향: 도메인 규칙을 데이터 가까이 옮길지 검토한다. DTO와 메시지 모델은 경계 모델로 남겨도 된다.

### Dead Code (죽은 코드)

- 의미: 더 이상 호출되지 않는 변수, 파라미터, 필드, 메서드, 클래스가 남아 있다.
- 살펴볼 질문: "언젠가 쓸지 몰라서" 남겨 둔 코드가 독자를 속이고 있는가?
- 개선 방향: 버전 관리 시스템을 믿고 제거한다. 제거 전 동적 호출이나 설정 기반 사용 여부는 확인한다.

### Speculative Generality (추측성 일반화)

- 의미: 아직 쓰이지 않는 확장 포인트, 파라미터, 추상화가 미리 들어 있다.
- 살펴볼 질문: 현재 요구보다 미래 상상을 더 많이 반영하고 있는가?
- 개선 방향: 지금 필요한 단순한 구조로 되돌린다. 실제 확장 압력이 생길 때 추상화를 도입한다.

---

## 8. Couplers: 결합도를 높이는 코드

Couplers는 클래스가 서로를 너무 깊게 알거나, 반대로 의미 없는 위임 층이 너무 많을 때 나타난다.

### Feature Envy (기능 욕심)

- 의미: 메서드가 자기 객체보다 다른 객체의 데이터와 규칙을 더 많이 다룬다.
- 살펴볼 질문: 이 로직이 정말 현재 클래스에 있어야 하는가, 데이터를 가진 객체 쪽이 더 자연스러운가?
- 개선 방향: Move Method, 도메인 메서드, 계산 책임 이동을 검토한다.

### Inappropriate Intimacy (부적절한 친밀함)

- 의미: 한 클래스가 다른 클래스의 내부 필드와 구현 세부사항을 지나치게 안다.
- 살펴볼 질문: 상대 내부 구조 변경이 곧바로 내 클래스 변경으로 번지는가?
- 개선 방향: 노출해야 할 역할만 인터페이스나 메서드로 드러내고 내부 접근을 줄인다.

### Message Chains (메시지 체인)

- 의미: `a.getB().getC().getD()`처럼 호출 체인이 길어 중간 구조에 의존한다.
- 살펴볼 질문: 중간 객체 구조가 바뀌면 호출부가 연쇄로 깨지는가? tibrv 메시지 객체를 여러 계층이 계속 들고 다니는가?
- 개선 방향: 필요한 의도를 표현하는 메서드나 내부 요청 객체를 도입한다. 경계의 원시 메시지를 핵심 로직까지 오래 끌고 가지 않는다.

### Middle Man (중개자)

- 의미: 클래스가 의미 있는 판단 없이 대부분의 일을 다른 객체에 넘기기만 한다.
- 살펴볼 질문: 이 위임이 경계를 보호하는가, 아니면 독자가 따라갈 파일만 늘리는가?
- 개선 방향: 의미 없는 위임 층은 줄인다. 외부 시스템 격리처럼 경계 역할이 명확하면 유지한다.

---

## 9. 레거시 코드에서 먼저 볼 코드 스멜

레거시 시스템에서 모든 스멜을 동시에 찾고 고치려 하면 실패하기 쉽다. 먼저 변경 위험이 큰 지점부터 본다.

1. 자주 바뀌는 God Class와 `Long Method`
2. 장애나 버그 수정 때 여러 파일을 동시에 건드리게 하는 `Shotgun Surgery`
3. 외부 객체 데이터를 과하게 끌어다 쓰는 `Feature Envy`
4. 깊은 체인과 메시지 객체 전달이 남는 `Message Chains`
5. 테스트 준비가 지나치게 많은 결합도 높은 코드

예를 들어 메시지 객체를 오래 들고 다니는 흐름은 이런 모습이 될 수 있다.

```java
public void process(TibrvMsg msg) {
    validate(msg);
    calculate(msg);
    save(msg);
    notify(msg);
}
```

처음에는 호출이 짧아 보이지만, 내부 메서드마다 메시지 필드 이름과 변환 규칙을 다시 알게 되면 변경 범위가 넓어진다.

```java
OrderRequest request = orderRequestMapper.from(msg);
orderProcessor.process(request);
```

경계에서 내부 요청 모델로 바꾸면 `Message Chains`, `Primitive Obsession`, 테스트 준비 비용을 함께 줄일 수 있다. 단, 이 변경도 현재 기능을 보호할 테스트와 작은 분리 순서가 있어야 안전하다.

---

## 10. 코드 리뷰에서 사용할 질문

코드 리뷰에서 "스멜입니다"라고만 말하면 대화가 막힌다. 다음 질문으로 변경 비용과 테스트 가능성을 함께 확인한다.

- 이 메서드는 한 화면을 넘어가면서도 흐름을 이해하기 쉬운가?
- 이 클래스는 몇 가지 이유로 바뀌는가?
- 이 변경 때문에 관련 없는 클래스가 함께 바뀌는가?
- 데이터를 가진 객체보다 다른 객체의 필드를 더 많이 묻고 있는가?
- 새 조건을 넣기 위해 같은 `if` 또는 `switch`를 여러 곳에서 고쳐야 하는가?
- 테스트를 쓰려면 DB, 외부 API, 메시지 객체, 현재 시간 같은 의존성을 너무 많이 준비해야 하는가?
- 지금 도입하려는 추상화가 실제 변경 압력을 줄이는가?

리뷰에서는 문제를 지적하는 문장보다 관찰과 제안을 연결하는 문장이 좋다.

```text
이 메서드는 검증, 계산, 저장을 함께 맡고 있어 수정 이유가 여러 개로 보입니다.
이번 변경에서 계산 로직만 먼저 분리하면 테스트 범위를 줄일 수 있을까요?
```

---

## 11. 정리

코드 스멜은 리팩토링을 시작하기 위한 관찰 언어다.

- `Bloaters`는 너무 커진 책임을 보여준다.
- `Object-Orientation Abusers`는 조건, 상속, 임시 상태의 어색함을 보여준다.
- `Change Preventers`는 변경이 번지는 비용을 보여준다.
- `Dispensables`는 줄여도 되는 부담을 보여준다.
- `Couplers`는 결합도와 위임의 균형을 묻는다.

우리 팀의 목표는 스멜 이름을 외우는 것이 아니다. 변경할 코드 주변에서 위험한 신호를 발견하고, 테스트로 동작을 보호하고, 작은 리팩토링으로 다음 변경을 더 싸게 만드는 것이다.

---

## 참고

- Martin Fowler, `Code Smell`
- Refactoring.Guru, `Code Smells`
