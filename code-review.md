# 코드리뷰를 왜 해야 하는가

## 1. 코드리뷰란 무엇인가?

코드리뷰(Code Review)는 코드가 운영 환경에 배포되기 전에 다른 개발자가 변경 내용을 검토하고 피드백을 제공하는 프로세스다.

코드리뷰의 목적은 단순히 버그를 찾는 것에만 있지 않다. 코드 품질을 높이고, 변경 의도를 팀이 함께 이해하며, 팀 전체의 개발 기준을 맞춰가는 활동이다.

좋은 코드리뷰는 "잘하는 사람이 꼼꼼히 봐주는 것"만으로 완성되지 않는다. 실수와 누락이 구조적으로 줄어들도록 리뷰 기준, PR 설명, 체크리스트, GitHub 기능을 함께 설계해야 한다.

---

## 2. 코드리뷰가 필요한 이유

### 2.1 버그 조기 발견

코드가 운영 환경에 배포되기 전에 결함을 발견하고 수정할 수 있다.

개발 후반이나 배포 이후에 버그를 발견하면 수정 비용이 커진다. 이미 다른 코드와 결합되었거나, 운영 데이터와 연결되었거나, 사용자 영향이 발생한 뒤일 수 있기 때문이다.

코드리뷰는 다음 질문을 통해 버그를 미리 발견하게 해준다.

- 정상 흐름뿐 아니라 실패 흐름도 고려했는가?
- null, 빈 값, 잘못된 입력을 처리하는가?
- 기존 동작을 깨뜨릴 가능성은 없는가?
- 예외가 발생했을 때 로그와 응답은 적절한가?

### 2.2 코드 품질 향상

코드리뷰는 팀의 코드 품질을 일정 수준 이상으로 유지하는 장치다.

- **일관성 유지**: 팀 전체가 같은 코딩 스타일과 규칙을 따르게 한다.
- **가독성 향상**: 다른 개발자가 이해하기 쉬운 이름, 구조, 흐름을 만들게 한다.
- **구현 개선**: 더 단순한 알고리즘, 더 명확한 책임 분리, 더 안전한 예외 처리를 제안받을 수 있다.
- **변경 비용 감소**: 지금은 동작하지만 다음 변경 때 위험해질 구조를 미리 발견할 수 있다.

코드리뷰에서 중요한 것은 취향 평가가 아니라 유지보수 비용을 낮추는 피드백이다.

### 2.3 지식 공유와 전파

리뷰는 팀원들이 서로의 코드를 통해 학습하는 통로다.

- 신규 팀원이 코드베이스와 아키텍처를 빠르게 이해할 수 있다.
- 특정 업무 지식이 한 사람에게만 남는 것을 줄일 수 있다.
- 기술적 논의가 기록으로 남아 비슷한 상황에서 참고할 수 있다.
- 팀의 좋은 코드 기준이 자연스럽게 공유된다.

코드리뷰는 "검사"가 아니라 팀 지식을 확산시키는 과정이다.

### 2.4 보안 취약점 방지

리뷰 과정에서 보안 취약점이나 위험한 코드 흐름을 발견할 수 있다.

예를 들면 다음과 같은 내용을 확인할 수 있다.

- 입력값 검증이 누락되지 않았는가?
- SQL, 파일 경로, 외부 명령 처리에 위험한 사용이 없는가?
- 민감 정보가 로그에 남지 않는가?
- 권한 체크가 필요한 API나 로직에 검증이 있는가?

모든 리뷰어가 보안 전문가일 필요는 없다. 다만 기본적인 위험 신호를 함께 보는 것만으로도 사고 가능성을 줄일 수 있다.

### 2.5 문서화 효과

PR과 리뷰 코멘트는 변경 이유와 의사결정 과정을 기록한다.

나중에 코드를 다시 볼 때 다음 내용을 확인할 수 있다.

- 왜 이 방식으로 구현했는가?
- 어떤 대안이 논의되었는가?
- 어떤 위험을 감수했는가?
- 어떤 부분을 추후 개선하기로 했는가?

리뷰 기록은 코드 옆에 남는 실무형 문서가 된다.

---

## 3. 좋은 코드리뷰는 능력이 아니라 설계다

리뷰 문화가 정착하지 못한 조직은 공통적으로 다음 문제를 가진다.

| 문제 | 결과 |
| --- | --- |
| 리뷰 기준의 부재 | 사람마다 보는 관점이 달라진다 |
| 스타일 리뷰로 편향 | 핵심 리스크보다 취향 평가에 집중한다 |
| 실패 관점 부재 | 정상 흐름만 보고 장애 시나리오를 놓친다 |
| PR 설명 부족 | 리뷰어가 변경 의도를 추측해야 한다 |
| 체크리스트 부재 | 매번 기억에 의존해서 누락이 생긴다 |

좋은 코드리뷰를 만들려면 다음 구조가 필요하다.

1. PR 작성자는 변경 의도와 영향 범위를 명확히 적는다.
2. 리뷰어는 팀 기준에 따라 위험 영역을 먼저 본다.
3. 단순 스타일은 자동화 도구나 컨벤션으로 줄인다.
4. 리뷰 코멘트는 문제, 이유, 제안을 함께 적는다.
5. 반복되는 지적은 문서나 템플릿으로 흡수한다.

---

## 4. 코드리뷰에서 먼저 볼 것

리뷰 시간은 제한되어 있다. 따라서 모든 줄을 같은 무게로 보는 것보다 위험도가 높은 부분을 먼저 봐야 한다.

### 4.1 동작 영향

- 기존 기능의 동작이 바뀌는가?
- 호출 순서나 상태 전이가 달라지는가?
- 예외 처리나 응답 코드가 달라지는가?

### 4.2 데이터 영향

- DB 저장, 수정, 삭제 로직이 바뀌는가?
- 배치나 대량 처리에서 데이터 정합성 문제가 생길 수 있는가?
- migration 또는 DB script가 필요한가?

### 4.3 외부 연동 영향

- 외부 API 요청/응답 형식이 바뀌는가?
- timeout, retry, 실패 응답 처리는 있는가?
- 메시지 포맷이나 필드명이 바뀌는가?

### 4.4 테스트 가능성

- 핵심 로직에 대한 테스트가 있는가?
- 버그 수정이라면 재발 방지 테스트가 있는가?
- 테스트 없이 확인해야 한다면 수동 검증 방법이 명확한가?

---

## 5. 코드리뷰를 잘 하기 위한 GitHub 활용법

### 5.1 PR 템플릿

PR 템플릿을 활용하면 리뷰어가 변경 사항을 빠르게 이해하고, 일관된 기준으로 리뷰할 수 있다.

```markdown
## PR 개요
- 어떤 작업을 했는지 간단히 설명해주세요.

## 관련 이슈
- SR No:
- Patch Item No:

## 주요 변경 사항

### 로직
- 주요 변경 파일/로직
- 버그 수정 또는 기능 추가 여부

### DB Script
- {zipfile}

## 체크리스트
- [ ] 단위 테스트를 작성했거나 기존 테스트가 통과합니다.
- [ ] 코드 컨벤션을 준수했습니다.
- [ ] 영향 범위와 검증 방법을 작성했습니다.

## 리뷰어 참고 사항
- 리뷰에 집중해야 할 부분
- 기타 공유하고 싶은 내용
```

PR 템플릿은 형식 자체보다 리뷰어가 먼저 봐야 할 정보를 빠뜨리지 않게 하는 데 의미가 있다.

### 5.2 Markdown 파일 활용

GitHub의 PR 설명, 이슈, 위키는 대부분 Markdown 형식을 지원한다. Markdown을 잘 활용하면 리뷰 문서를 더 읽기 쉽게 만들 수 있다.

| 기능 | 문법 | 예시 |
| --- | --- | --- |
| 제목 | `#`, `##` | `## 주요 변경 사항` |
| 강조 | `**텍스트**` | **중요** |
| 코드 | `` `code` `` | `OrderService` |
| 코드 블록 | <code>```java</code> | Java 코드 예시 |
| 목록 | `-` 또는 `1.` | `- 항목` |
| 체크리스트 | `- [ ]` | `- [ ] 테스트 완료` |
| 링크 | `[텍스트](URL)` | `[GitHub](https://github.com)` |
| 표 | `| col1 | col2 |` | 비교표 |

코드리뷰에서는 특히 코드 블록, 체크리스트, 표가 유용하다.

### 5.3 File Changed 탭 활용

GitHub의 `Files changed` 탭은 코드리뷰의 핵심 화면이다.

활용 방법은 다음과 같다.

- 리뷰할 라인을 선택해 코멘트를 남긴다.
- 여러 줄을 드래그해 문맥 단위로 코멘트를 남긴다.
- 단순 수정은 `Suggestion` 기능으로 직접 제안한다.
- 큰 변경은 파일 단위로 먼저 훑고, 위험도가 높은 파일부터 자세히 본다.

라인 코멘트는 "어디가 문제인지"를 정확히 공유할 수 있다는 장점이 있다.

### 5.4 Diff와 Suggestion 활용

단순 오타, 조건식 수정, 이름 변경처럼 명확한 수정은 설명만 남기기보다 Suggestion을 사용하는 편이 빠르다.

```suggestion
if (request == null) {
    throw new IllegalArgumentException("request must not be null");
}
```

다만 설계 방향이나 업무 규칙처럼 논의가 필요한 내용은 Suggestion보다 질문과 근거를 남기는 것이 좋다.

### 5.5 이모지 활용

이모지는 리뷰의 의도를 빠르게 전달할 수 있다.

예를 들면 다음처럼 사용할 수 있다.

- `:question:` 질문 또는 확인 요청
- `:warning:` 위험 가능성
- `:white_check_mark:` 확인 완료
- `:bulb:` 개선 아이디어
- `:memo:` 문서화 요청

이모지는 보조 수단이다. 중요한 리뷰 코멘트는 반드시 이유와 제안을 함께 적어야 한다.

---

## 6. 좋은 리뷰 코멘트 작성법

좋은 리뷰 코멘트는 세 가지를 포함한다.

1. 무엇이 문제인지
2. 왜 문제가 될 수 있는지
3. 어떻게 바꾸면 좋을지

예시는 다음과 같다.

```text
이 메서드는 결제 요청과 로그 저장을 함께 처리하고 있어 실패 시 영향 범위를 판단하기 어렵습니다.
결제 요청 결과를 반환한 뒤 로그 저장은 별도 메서드로 분리하면 테스트 범위가 더 명확해질 것 같습니다.
```

피해야 할 코멘트는 다음과 같다.

```text
이건 별로입니다.
이렇게 하지 마세요.
왜 이렇게 했나요?
```

리뷰 코멘트는 사람을 평가하는 문장이 아니라 코드와 변경 위험을 다루는 문장이어야 한다.

---

## 7. 우리 팀에서 적용할 기준

우리 팀의 코드리뷰는 다음 기준으로 시작할 수 있다.

- PR 작성자는 변경 목적, 주요 변경 파일, 테스트 여부를 반드시 적는다.
- 리뷰어는 스타일보다 동작 영향, 데이터 영향, 실패 시나리오를 먼저 본다.
- 단순 수정은 Suggestion으로 빠르게 제안한다.
- 반복되는 리뷰 코멘트는 PR 템플릿이나 체크리스트에 반영한다.
- 리뷰에서 논의된 결정은 PR 설명이나 코멘트에 남긴다.

코드리뷰는 한 번에 완벽하게 정착되지 않는다. 중요한 것은 팀이 같은 기준으로 리뷰하고, 반복되는 누락을 구조적으로 줄여가는 것이다.

---

## 8. 세미나 중 던져볼 질문

- 우리는 지금 리뷰에서 무엇을 가장 많이 보고 있나요?
- 스타일 코멘트와 위험 코멘트 중 어느 쪽이 더 많은가요?
- PR 설명만 보고 변경 의도와 영향 범위를 이해할 수 있나요?
- 리뷰어가 가장 먼저 봐야 할 파일이 PR에 표시되어 있나요?
- 버그 수정 PR에 재발 방지 테스트가 포함되어 있나요?
- 반복되는 리뷰 코멘트를 템플릿으로 줄일 수 있나요?

---

## 9. 정리

코드리뷰는 배포 전 결함을 찾는 활동이면서, 팀의 코드 품질 기준을 맞추는 활동이다.

좋은 코드리뷰는 개인의 꼼꼼함에만 의존하지 않는다. PR 템플릿, Markdown, File Changed, Suggestion, 체크리스트 같은 도구를 활용해 리뷰어가 중요한 내용을 놓치지 않도록 설계해야 한다.

결국 코드리뷰의 목표는 "누가 더 잘했는가"를 판단하는 것이 아니라, 팀이 더 안전하게 변경하고 더 빠르게 학습할 수 있는 구조를 만드는 것이다.
